/// The entrypoint: everything the app is made of arrives through [runTom].
library;

import 'package:tom_desktop/bootstrap/run_tom.dart';

/// Starts the app with no extra modules.
void main() => runTom();
