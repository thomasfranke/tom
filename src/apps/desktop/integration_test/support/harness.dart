/// Everything a scenario needs, in one import — kept inside `tom_desktop` so
/// the run happens in the app's own native runner rather than a second one.
library;

export 'e2e_module_impl.dart';
export 'evidence.dart';
export 'fixture.dart';
export 'robot.dart';
export 'scenario.dart';
