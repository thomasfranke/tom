# The repository above the space

The space is `docs/`; this file is the repository's own, outside it. It is
here so the fixture is the normal case — a folder inside a repository, not a
repository opened at its root (rule 12).
