# TOM end-to-end fixture

Built by `tom e2e prepare` from `src/apps/desktop/integration_test/fixtures/not-a-repository/`. It is staged here rather than in the repository because it exists to be *outside* one: git looks upward, so a folder inside the project would be found by it.
