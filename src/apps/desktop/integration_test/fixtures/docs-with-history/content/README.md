# The repository above the space

The space is `docs/`; this file is the repository's own, outside it — so the
fixture is the normal case, a folder inside a repository rather than a
repository opened at its root (rule 12).
