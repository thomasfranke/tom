name: tom_core
description: "Result, AppFailure and the primitives every layer shares. Depends on nothing."
version: 0.1.0
publish_to: none
repository: https://github.com/thomasfranke/tom

environment:
  sdk: ^3.12.0

resolution: workspace

# No dependencies at all: this is the bottom of the graph, and it is the
# only package in the workspace that can say so.

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
