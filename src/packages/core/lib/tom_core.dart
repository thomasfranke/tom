/// Result, AppFailure and the primitives every layer shares. Depends on
/// nothing.
///
/// Nothing outside `lib/src/` is importable from another package, so this
/// file is the whole public surface.
library;
