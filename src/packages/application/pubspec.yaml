name: tom_application
description: "Use cases: orchestration between the domain and its repositories."
version: 0.1.0
publish_to: none
repository: https://github.com/thomasfranke/tom

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  tom_core:
    path: ../core
  tom_domain:
    path: ../domain

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
