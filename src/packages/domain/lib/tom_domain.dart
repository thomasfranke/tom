/// Entities, value objects, repository contracts and domain services. Knows
/// no framework, no git, no disk.
///
/// Nothing outside `lib/src/` is importable from another package, so this
/// file is the whole public surface.
library;
