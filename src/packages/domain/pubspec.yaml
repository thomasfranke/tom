name: tom_domain
description: "Entities, value objects, repository contracts and domain services. Knows no framework, no git, no disk."
version: 0.1.0
publish_to: none
repository: https://github.com/thomasfranke/tom

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  freezed_annotation: ^3.1.0
  tom_core:
    path: ../core

dev_dependencies:
  build_runner: ^2.15.1
  freezed: 3.2.5
  lints: ^6.0.0
  test: ^1.25.6
