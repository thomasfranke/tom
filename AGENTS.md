# AGENTS.md

Entry point for any AI agent working in this repository (platform-agnostic
by design — Claude Code, Codex, Cursor, or anything that reads this file).
Read before any task.

## What this project is

**TOM — Team-Oriented Markdown** *(binary: `tom`)* — a desktop Git client specialized in markdown documentation. Flutter Desktop (Windows/macOS/Linux), open source (MIT), open-core business model.

**Pitch:** "A GitHub Desktop for docs". The differentiator is the Git workflow as a first-class citizen (*rendered* markdown diff, branch/commit/push without ceremony) — NOT the editor, which is deliberately simple (source + preview, no WYSIWYG).

## Source of truth

`docs/` is two folders and two files, split by audience (product vs. technical), with `about.md` as the shared context above the fork and `roadmap.md` as the order things arrive in. **Before suggesting architecture, dependencies or scope, consult:**

- [`docs/about.md`](docs/about.md) — what the project *is*: the bet, principles, personas, killer features and **non-goals** (respect them!). Short by design; read it first.
- [`docs/product/`](docs/product/README.md) — **the source of truth for what each feature must do**, in non-technical language, one folder per feature (`doc.md` + `mocks/`). Maintained by stakeholders, kept current as the app evolves. Check the relevant `doc.md` before implementing or changing a feature's behavior, and flag it if the code diverges from what's written there.
- [`docs/technical/`](docs/technical/README.md) — how it is built: `layers.md` (the graph and what enforces it), `flows.md` (runtime behaviour), `domain-model.md`, `dependencies.md` (stack with licenses), the development process, `decisions/` (ADRs — a new decision is a new file, changing one requires an explicit revision) and `design/` (wireframes and the visual language).
- [`docs/roadmap.md`](docs/roadmap.md) — spikes, phases and milestones, in order — whatever is out of the MVP stays out. **There is no task queue and no spec folder**: work is picked from the roadmap, or from what the maintainer asks for directly.
- **Read selectively.** Consult only the files relevant to the task at hand; never load `docs/` wholesale. The index above exists so the right file can be picked without reading the rest.
- **The canonical form of a rule is the dartdoc of the code that implements it** — `docs/technical/` states the rule, the code shows it. When the two disagree, the code is right and the doc is a bug.

## Human gates

One checkpoint, named explicitly rather than implied. Everything else is agent work, autonomously — and **no process artefact is a prerequisite for doing the work**: there is no task to open, no spec to draft, no approval to wait for. What is asked for gets built.

| Transition | Who |
|---|---|
| Changing a doc under `docs/` | Human writes it or reviews it before merge. Agents may draft; a doc never merges on agent approval alone. |

The one thing that still applies to any change: if the request is ambiguous
enough that two readings produce different work, ask — don't improvise scope.

## Skills — load on the matching trigger

Operational procedures live in `.ai/skills/`, one folder per skill, each with a
`SKILL.md`. **They are not auto-discovered** — no agent loads them on its own,
so this table is what activates them. Check it before acting.

| Skill | Load it when | Covers |
|---|---|---|
| [`tom-code-review`](.ai/skills/tom-code-review/SKILL.md) | reviewing a diff, a branch or a PR; running `/code-review`, `/simplify` or `/security-review` here; the user says "review this", "does this follow the architecture", "is this ok to merge" — or you are about to open a PR and are reading your own branch | What the tooling already proves versus what only a reader can check — the layer graph, `Result` and the failure hierarchies, Freezed, typed paths, the capability contracts, the session and module patterns — package by package, the test layout, the docs that must move with the change, the general canon (Effective Dart, Clean Architecture, DDD per Decision 15, community conventions) and its precedence under the project's own choices, and which settled decisions are never a finding |
| [`tom-comments`](.ai/skills/tom-comments/SKILL.md) | writing a new class, method or library and reaching for a comment; judging in review whether a comment earns its lines; the user says "the comments are too long", "keep them cohesive", "trim this", or asks what belongs in a dartdoc — and before adding a paragraph to any existing dartdoc | The three shapes (the `library` line, the dartdoc, the why-comment inside a body), what is not a comment, the two exceptions that earn extra lines and the twelve-line ceiling on them, and a real before/after trim from this repository |
| [`tom-git-workflow`](.ai/skills/tom-git-workflow/SKILL.md) | committing, staging, writing a commit message, creating a branch, opening or merging a PR, cutting a release, applying a hotfix — or the user says "commit this", "push", "open a PR", "ship it", or asks which branch to target | Trunk-based on `main`, branch naming, Conventional Commits with the monorepo's scopes, squash policy, releases as tags, what differs in `tom-pro` |
| [`tom-pr-writer`](.ai/skills/tom-pr-writer/SKILL.md) | about to open a PR, "write the PR", "draft a PR description", finishing a branch, reviewing or rewriting an existing description, writing the squash commit message | Inspecting the real diff before writing, title = the Conventional Commit the squash will produce, the What/Why/Notes template |
| [`tom-wireframes`](.ai/skills/tom-wireframes/SKILL.md) | drawing, editing or reviewing a wireframe, mock or screen layout; "wireframe", "mock up a screen", "draw the layout", "excalidraw"; adding a screen to `docs/technical/design/` | The shared drawing kit, design tokens, components, and the rules that keep every screen looking like one product |

How they are maintained, and why `tom-pro` inherits them by relative path
instead of copying: [`.ai/skills/README.md`](.ai/skills/README.md).

## Non-negotiable rules (summary of the decisions)

1. **MIT license** — NO AGPL/GPL dependency (no `appflowy_editor`). Check the license of every new package (Decision 1).
2. **Git through the system binary** (`Process.run`) behind contracts — do not suggest libgit2/FFI at this phase (Decision 2).
3. **No WYSIWYG** — the editor is source + preview (Decision 3).
4. **No `dartz`** — Result with native sealed classes, exhaustive switch, early return (Decision 5); rules in `docs/technical/layers.md#errors-across-boundaries`; the types live in `src/packages/core/lib/src/` and `src/packages/domain/lib/src/`. Inline try/catch mandatory in every use case.
5. **No navigation package** — plain `Navigator` for dialogs only (Decision 6).
6. **External dependencies isolated behind contracts** in infrastructure (Decision 7); Riverpod ONLY in presentation + bootstrap/di — no `Ref` in any other layer; constructor injection (`docs/technical/flows.md#wiring-three-lifetimes`).
7. **One package per layer** (Decision 14, revising 8): `src/packages/{core,domain,application,infra,data,presentation}` are pure Dart with NO Flutter in the pubspec; only `src/apps/desktop` has Flutter. A layer may depend only on the ones below it — enforced by the pubspecs, by `depend_on_referenced_packages`/`implementation_imports` as errors, and by `src/test/architecture_test.dart`.
8. **Files are the truth** — never propose a database or state that is not rebuildable from the `.md` files on disk. The FTS5 index is a cache.
9. **Telemetry is opt-in** — observability behind a contract, no-op by default; no data leaves the user's machine (Decision 11).
10. **Extensible shell** — panels are ALWAYS registered through `TomModule`/`PanelDescriptor`, never hardcoded in the shell, including the built-in ones (Decision 12; `docs/technical/flows.md#panels-are-registered-never-hardcoded`).
11. **The public repo documents the free product only** — no tier catalogue, no pricing, no licensing mechanics, no reference to a private repository. Commercial modelling lives outside this repo (Decision 4).
12. **A space is a folder, not a repository** — `SpaceEntity` carries `root` and `repositoryRoot` separately; git runs against the repository, navigation and search stay in the folder.
13. **Freezed is mandatory for immutable data** — entities, value objects with more than one field, presentation view-state, and sealed hierarchies; no hand-written `==`/`copyWith` once a class qualifies (Decision 16). Single-field identifier/path value objects (`BranchNameValueObject`, `CommitShaValueObject`…) are the one recommended exception — an `extension type` fits better there.

## Stack (summary — details in docs/technical/dependencies.md)

Riverpod (codegen) + Freezed · `markdown` (AST) · `diff_match_patch` · `re_editor` (spike pending) · `sqlite3` + FTS5 · `watcher` · `window_manager` / `file_selector` · `dart:io Process` for Git. Why Dart rather than a web stack, and where Rust could still enter via FFI: Decision 13.

## Code structure

See `docs/technical/layers.md` (source of truth). The Dart workspace lives under `src/`; run everything through `dart run tool/tom.dart <command>` from the repository root (`make` targets are one-line shortcuts over the same CLI, and `tool/` never calls back into `make`).

```
core ← domain ← application ← presentation
  ↑       ↑                        ↑
  └─── infra ← data ─────────────  desktop
```

`core` holds `Result`, the `AppFailure` marker and the ports every layer needs — mechanism, never product vocabulary; `domain` holds entities, value objects, the sealed failure hierarchies (`GitFailure`, `DocumentFailure`, `SearchFailure`), repository contracts and BlockDiffer; `application` use cases; `infra` capability contracts *and* their implementations, one folder per capability holding the contract, its failures and a subfolder per implementation named after the dependency ([Decision 24](docs/technical/decisions/024-a-capability-is-a-folder.md): `git_client/dart_io/dart_io_git_client_impl.dart`); `data` the DTOs that cross those contracts, the data sources that obtain them, the parsers and the repository implementations ([Decision 25](docs/technical/decisions/025-a-repository-reads-through-a-data-source.md): a repository orchestrates and translates, a source obtains); `presentation` the space session and notifiers, pure Dart so it cannot reach a widget; `apps/desktop` the composition root and the widgets.

## Conventions

- **Git workflow (branches, commits, PRs, releases): see the `tom-git-workflow` skill** in `.ai/skills/` — consult it before committing, branching, or opening a PR. Human-facing version: `CONTRIBUTING.md`.
- Conventional Commits + semantic versioning. Trunk-based: `feat/*` → PR into `main` (squash) → tag publishes. No `dev` branch. `main` is publishable; production is the most recent tag.
- **Incomplete work integrates behind a build-time feature flag**, never on a long-lived branch (`CONTRIBUTING.md`, "Feature flags"). Disabled = unreachable, and every flag has a removal target.
- Documentation updated in the same PR that changes behavior (dogfooding: this project exists for that)
- **If a PR changes a product's user-facing interface, it updates that product's mock in the same PR** (`docs/product/<group>/<feature>/mocks/`) — a mock that no longer matches what shipped is worse than no mock, see [docs/product/README.md](docs/product/README.md)
- A new architecture decision → a new file in `docs/technical/decisions/` following the existing format
- **All documentation and code in English** (identifiers, comments, docs)
- **A comment is two or three lines** — what the thing is, then why it is that way ([layers.md](docs/technical/layers.md#inside-a-package)). Longer is an exception that earns itself: a rule with no other home, or a trap worth an afternoon

## Current status

**Phase 0 — Spikes.** No milestone has been built. The workspace skeleton is in place and the error model is real code: `Result`, `AppFailure` and `Observability` in `tom_core`; the sealed `GitFailure`/`DocumentFailure`/`SearchFailure` hierarchies in `tom_domain`; `FilesystemFailure` and `GitClientFailure` in `tom_infra` (96 tests across the workspace). Every `AppFailure` hierarchy is now Freezed — `tom_core`, `tom_domain` and `tom_infra` each carry `freezed`/`freezed_annotation`/`build_runner`, generated equality replaces the hand-written `==`/`hashCode`, and `--ignore-files` keeps `*.freezed.dart` out of the coverage gate (`tool/src/commands/coverage_gate.dart`, `tool/src/commands/run_tests.dart`, `tom coverage`). `docs/architecture/` was consolidated from 13 files into four, and `docs/` then settled on `about.md` + `roadmap.md` + `product/` + `technical/`, with `architecture/`, `process/`, `decisions/` and `design/` folding into `technical/` and `products/` becoming `product/`. The domain model is deliberately partial (`docs/technical/domain-model.md`) — `BlockValueObject` in particular is an *output* of Spike B; do not design `BlockDiffer` before the spike reports. The task/spec process was removed in full: `docs/tasks/`, `docs/specs/` and the leftover `work/` queue are gone, along with the schemas and the selection algorithm that went with them — work is picked from `docs/roadmap.md` or asked for directly, and nothing has to be filed before it can be built. `tom_infra` holds two of the five capabilities `layers.md` names: `filesystem/` and now `git_client/` — the `GitClient` contract, the sealed `GitClientFailure`, and `dart_io/` driving the system binary with a serialized per-space queue, a forced environment that leaves no prompt to answer (`GIT_TERMINAL_PROMPT`, `GIT_ASKPASS`, `SSH_ASKPASS_REQUIRE`, plus `GIT_OPTIONAL_LOCKS`/`LC_ALL`), timeouts that kill the process, and stderr recognised into typed failures (46 tests, most of them integration against real `git init` repositories and a local bare remote). **Text crosses that contract, never an entity**: `GitStatusValueObject`, `CommitEntity` and `BranchEntity` are domain types (`docs/technical/domain-model.md`) and the parsers that build them belong to `tom_data`, so every method documents the exact format it returns. Two doc deltas wait on the human gate: `layers.md` still names the implementation folder `process/` (the code says `dart_io/`, after the capability's package, matching `filesystem/dart_io/`), and Decision 2 still calls the contract `GitClientInterface`. `markdown_parser/` and `text_differ/` sit behind Spike B and `search_index/` is M2. File watching and settings are used by the roadmap but are not yet declared capabilities in `layers.md`. `Filesystem` has since grown the two operations M0 needs beyond read and write: `listDirectory` (one level or recursive, sorted, links reported and never followed, nothing filtered — hiding `.git/` is the file tree's policy, not the capability's) and `directoryExists`, which answers false for a folder that is gone but fails when the machine will not say. `FilesystemEntry` and its type enum live in their own files beside the interface, which holds nothing but the contract. A review of `tom_domain` and `tom_data` closed nine findings, all with tests: `RepoRelativePathValueObject` and `BranchNameValueObject` now validate segment by segment (no `..`, no empty segment, no drive letter; no `@{`, no component starting or ending in `.`, `.lock` per component) — the branch rules were checked against `git check-ref-format --branch` and are a strict subset of it; `GitLogParser` trims the commit body on the right only, because leading indentation is markdown content; a porcelain `2` record is a rename *or a copy*, so `previousPath` is attached only to a rename; every failure variant now carries its hierarchy's prefix (`DocumentPermissionDenied`, `GitMergeConflict`, `SearchIndexCorrupted`…), matching what `tom_infra` already did; the `-z` NUL is `GitClient.nulSeparator` rather than a second copy in the parser. Two of the nine were design, not slips: `Commit.date` is a `CommitDate` (instant + the author's offset) because `DateTime` silently discards the offset `%aI` carries, and `GitStatus.isDetached` is a field rather than `branch == null`, because an unparseable branch name also produced null and was being reported as a detached `HEAD`. The ninth is open by decision: Freezed compares collections element-wise but does not copy them, so `GitStatus.entries` and `GitMergeConflict.conflictedFiles` are handed over rather than copied — the producer passes `List.unmodifiable` and the contract is stated on the fields, but making it structural needs an immutable-collection package (license check under rule 1, plus an ADR) and was left for the maintainer. A review of `tom_infra` closed eleven findings, nine of them with a test that fails without the fix. On `GitClient`: every path crossing the contract is now repository-root relative in both directions — `stage`, `unstage` and `log(path:)` send `:(top,literal)` pathspecs, so a path read from `status()` can be handed straight back, which was broken for every space that is a folder *inside* a repository rather than the repository itself (rule 12, i.e. the normal case); the conflicted paths of a `GitClientMergeConflict` come from the index (`diff --diff-filter=U`) rather than from the `CONFLICT` lines, which spell out a content clash and nothing else — a modify/delete conflict used to be announced with no file named at all; a killed command stops waiting on pipes a surviving grandchild still holds, which left the space's serialized queue stuck for as long as that process lived, the very stall the timeout exists to prevent; `log()` on a branch with no commits yet returns nothing instead of failing; and the probe that tells "no git" from "no folder" can no longer throw a `FileSystemException` out of the package. On `Filesystem`: `writeFile` creates the directories its path needs and lands through a temporary sibling and a rename, so a crash mid-save cannot leave a truncated document; `readFile` decodes UTF-8 explicitly and refuses anything else as the new `FilesystemNotUtf8` rather than writing replacement characters back over bytes it could not read (a capability decision — `docs/product/` rules on no encoding yet); a recursive `listDirectory` skips a folder the machine will not open instead of losing the whole tree, and names the path that actually failed; and the translation reads `dart:io`'s exception types instead of raw `errno`, which had POSIX `EIO` reported to the user as a permission problem. 77 tests in the package, 98% line coverage. `tom_data` now fulfils its first contract: `GitRepository` (the domain's git vocabulary — entities in, entities out) and `GitRepositoryImpl` over the `GitClient` capability, where the three parsers turn text into `GitStatusValueObject`/`CommitEntity`/`BranchEntity` and a switch that is exhaustive by construction turns every `GitClientFailure` into a `GitFailure`; a failure the capability cannot produce passes through untouched rather than being dressed up as a command that was never run (86 tests in the package, 100% line coverage, unit for the translation and integration against real `git init` repositories). Two `GitFailure` variants were promoted out of infrastructure's vocabulary — `pushRejected`, which the product shows as its own outcome, and `timedOut` — plus `DocumentFailure.notUtf8` to match `FilesystemNotUtf8`; `GitDetachedHead` is still produced by nothing, because git commits happily on a detached `HEAD` and refusing is a use case's policy, not a command's failure. The integration test found one real defect in `tom_infra`, fixed with a test that fails without it: `unstage` ran `git restore --staged`, which rebuilds the index from `HEAD` and is therefore fatal on a repository with no commits yet — the normal state of a space opened on a fresh `git init` — and now falls back to taking the path back out of the index, `--ignore-unmatch` so both branches behave alike. The domain gained the entities the model already called settled: `SpaceEntity` (`root`, `repositoryRoot`, `name`, with the enclosure invariant asserted and the assertion's helpers as private top-level functions, since a Freezed `@Assert` is evaluated in the generated part and cannot see a static) and `DocumentEntity`, plus `SpaceRelativePathValueObject` beside `RepoRelativePathValueObject` — one syntax rule shared, two types, and `SpaceEntity` as the only converter, `toSpaceRelative` answering null for what the space does not contain. The document side followed, as two contracts rather than one, because they fail differently: `DocumentRepository` reads and writes one file (`DocumentFailure`, which gained an `operationFailed` fallback), `SpaceRepository` lists the folder (the new sealed `SpaceFailure` — `folderMissing`, `accessDenied`, `operationFailed` — which is also the vocabulary Home will need for a recent space whose folder went away). Both are per space and fulfilled over `Filesystem`, with `Space.relativize` as the way back from the absolute paths a listing reports. `SpaceEntryValueObject` is the tree's line — a path and its kind, a link reported as itself — and `isDocument` is what separates what the tree draws from what the editor opens. **The file tree's `.git/` policy lives in `SpaceRepositoryImpl`, and the load-bearing half is that it never *descends*:** a recursive listing from the capability walks into `.git/` before anything can filter it, and a mature repository keeps more entries there than the product will ever show, so the walk goes one level at a time and decides before descending — a unit test asserts the listing was never even asked for that folder. `.git` is hidden whatever it turns out to be, since a worktree or a submodule has it as a file. 122 tests in `tom_data`, 100% line coverage; 122 in `tom_domain`. **Spike A is answered: `re_editor` carries source mode** ([Decision 18](docs/technical/decisions/018-source-mode-uses-re-editor.md)), and the `TextField` fallback is withdrawn. It was decided by measurement rather than by reading: a harness at `src/apps/desktop/lib/spikes/` — a second entrypoint, unreachable from `main.dart`, not a flag — loads a generated 2853-line/131KB markdown document and drives typing at 60 characters a second, and the same harness runs the fallback as a control arm on the same machine in the same build mode. `re_editor` builds at 4.4ms p50 and misses 0.6% of frames; the `TextField` builds at 38.8ms and misses 96.2%, because it lays out the whole document as one paragraph on every keystroke. At ten times the file the roadmap asked for (29,637 lines, 1.37MB) it still types at 6.6ms p50. Find and replace were measured too rather than left to a human — `CodeFindController` is drivable: 89 matches, replace-all leaving zero. What the package does **not** ship is the find panel and the selection toolbar, roughly 250 lines of UI in its own example, which is the right split since that chrome has to be in TOM's visual language anyway. The harness is kept, with the removal target stated in its own dartdoc: it goes when M0 ships source mode with its own tests, and until then it is what re-answers the question when a Flutter upgrade breaks the package — which the spike already hit on `^0.7.0`, where `TextInputClient` had grown `onFocusReceived`. Two things found on the way that are not about the editor: the macOS runner's deployment target was 10.15, which Xcode 27 refuses (now 12.0), and **macOS profile and release builds are blocked** by a `flutter_tools` 3.44.5 / Xcode 27 incompatibility — the tool verifies the engine framework with `lipo <file> -verify_arch arm64 x86_64` and Xcode 27's `lipo` accepts only one architecture there. Debug is unaffected, so every number above is a floor; it blocks M3 packaging and is an open question in the roadmap. **Spike B is answered too: the `markdown` package carries the blocks** ([Decision 19](docs/technical/decisions/019-blocks-come-from-the-markdown-package.md)), and neither fallback — a hand-written block parser, a Rust parser over FFI — is taken. `BlockValueObject` moves from *Open* to *Settled* in `docs/technical/domain-model.md`: **a span, its text and its kind**, never a package AST node, with the structure re-derived by parsing the span because that costs 0–3%. The report at `src/apps/desktop/lib/spikes/spike_b/` runs over this repository's own 50 documents and 848 blocks, plus one fixture that exists because the corpus turned out not to exercise the question that mattered. Findings, in the order they bite: the AST has **no source positions**, but they are recoverable for every block — and **wrapping a `BlockSyntax` to recover them silently changes the parse**, because the package's syntaxes recognise each other *by type* (`ParagraphSyntax` asks whether what interrupted it `is SetextHeaderSyntax`, and behind a decorator that answers false, so a setext heading quietly becomes a paragraph); extending each syntax works, at nineteen subclasses, and section 0 of the report is the check that the parse did not change. A block also does not always start where the parser is standing — a setext heading's text is consumed and handed back before the heading claims it, and `linesToConsume` is how far back it really began. Granularity is top level and that is the right size: a list is one block, a table is one block, 668 of 848 blocks are a single line and none exceeds 20. **There is no stable identity** — 288 distinct heading paths for 848 blocks, 285 of them holding more than one — so `BlockDiffer` aligns by position and similarity, which is the constraint a design session would most likely have got wrong. Isolated rendering works for 848 of 848, reference links included once the document's `linkReferences` map travels with the block; **footnotes are the one thing that breaks** (`[^ref]` renders as literal text, and the parser also synthesises a footnotes `section` node with no source at all), which is M2's problem with a failing case waiting. **Phase 0 is over, and M0 has started: the workspace shell is built.** `src/apps/desktop/` is no longer a placeholder — `main.dart` is three lines calling `runTom()`, and everything the app is made of arrives through it. `TomModule` (id, panels, provider overrides), `PanelDescriptor` (id, title, placement, builder, order), `PanelPlacement` (explorer · document · aside · statusBar) and a `PanelRegistry` the shell reads; `TomShell` names no panel and imports none. **The built-in panels go through the same path as a stranger's**, registered by `CoreModuleImpl` — which is what a widget test asserts twice, once with `CoreModuleImpl` and once with a module written in the test, plus a third that mounts the shell with *no* modules and checks the screen is empty, because anything still drawn there would be a panel the shell hardcoded. 17 widget tests. Two dependencies, both MIT: `flutter_riverpod` ^3.4.3 (note: Riverpod 3 does not export `Override` from its main barrel — it is in `flutter_riverpod/misc.dart`) and `window_manager` ^0.5.2 for the title and a minimum window size. The colour roles of `docs/technical/design/visual-language.md` are a `ThemeExtension` so no widget ever asks which mode it is in, and the layout's fixed numbers come from the wireframe kit (`TOP_BAR` 52, `STATUS_BAR` 32, `EXPLORER_W` 220, `PAD` 20) — when code and wireframe disagree, the wireframe is right. `window_manager` is the first plugin with native code, so macOS now has a `Podfile`: it needs `platform :osx, '12.0'` **and** a `post_install` hook raising `MACOSX_DEPLOYMENT_TARGET` on every pod target, because Flutter's own `flutter_additional_macos_build_settings` writes 10.15 back over the platform line and Xcode 27 refuses anything below 12.0. **Home is built, and with it the graph is whole for the first time**: a folder picked on screen reaches git through every layer and comes back as a `SpaceEntity`. `SpaceRepository` became app-wide and gained `open` — a `SpaceEntity` is data, so it travels as an argument rather than as a lifetime, and a repository that could list a space but not produce one would have needed a factory beside it, which [Decision 15](docs/technical/decisions/015-ddd-is-applied-selectively.md) rules out. Opening asks the disk *before* git, because the two failures send the user somewhere different: a folder that is gone is `SpaceFolderMissing` (Home offers to forget it), a folder outside any repository is `GitNotARepository` (Home explains it, and TOM never runs `git init` for anyone). `GitClientFor` is a typedef in `tom_infra` — a folder picked at runtime needs a client built for it, and a factory class there would be a name wrapped around a constructor call. `tom_application` has its first code: a `UseCase` mixin holding the one standardized `try/catch`, so "standardized" and "copied into every file" stay different things, and `OpenSpace`, `ListRecentSpaces`, `ForgetRecentSpace`. **Remembering is part of opening** rather than a second call a screen could forget, and it never fails the open. `tom_presentation` has its first code too: `HomeState` (`initial/loading/ready/failed/opened`, sealed) and `HomeNotifier`, which reads its use cases from providers *it declares and the composition root overrides* — presentation names what it needs and cannot see what satisfies it. A new capability: **`Settings`, as one JSON file in the platform's application-support folder**. Not `shared_preferences` — it is a Flutter plugin and `tom_infra` is pure Dart, so taking it would push the capability up into the app to store a list of folder paths; `dependencies.md` already named JSON-in-app-support as the alternative and that is what this is. `layers.md` now lists settings among the capabilities. Three traps worth remembering: a Riverpod provider nobody listens to is disposed as soon as it is read, so a notifier that loads in a microtask needs a listener before it will do anything (it cost two red tests); `prefer_const_constructors` asked for a `const` empty map that `JsonFileSettingsImpl` then tried to write into, which is why that one line carries a documented `ignore`; and `MaterialApp` animates a theme change, so a test comparing light against dark has to settle first. One thing the first real run found, and it was not about Home: **the macOS app cannot be sandboxed** ([Decision 20](docs/technical/decisions/020-the-macos-app-is-not-sandboxed.md)). Flutter's generated runner enables the App Sandbox, which forbids executing `/usr/bin/git`, reading `~/.gitconfig` and reaching the user's SSH keys — every one of them load-bearing for [Decision 2](docs/technical/decisions/002-git-via-system-binary.md), whose whole claim is that authentication comes free because it is the user's own git running. It surfaced as the folder picker doing nothing. The sandbox is now off in both entitlements with the reason in the files, and the cost is the Mac App Store, which the roadmap had already given up by choosing a dmg. **The file tree is built, and the space session arrived with it.** [Decision 9](docs/technical/decisions/009-space-session-is-single-source-of-truth.md) had no code until now, and the open space lived in `HomeState.opened` — which made a screen the owner of the fact the whole window is built on. `HomeOpened` is gone: `SpaceSessionNotifier` holds the open `SpaceEntity` and the open document, `HomeNotifier` *writes* to it, and `_WindowContents` reads **that** to choose between Home and the shell. The session is `keepAlive` on purpose — Riverpod disposes a provider as soon as nothing listens, and this one is written by the screen that is on its way out, so a disposal between the write and the first panel would leave the window on Home with nothing to say. Panels derive with `select`: the tree watches `session?.space` and not the session, which is what keeps clicking a document from re-reading the folder (a test asserts the listing happened exactly once). `ListSpaceEntries` is the use case; `FileTreeNotifier`/`FileTreeState`/`FileTreeRow` are the panel's state. The whole space is read in one call — affordable only because the walk never descends into `.git/` — so expanding a folder is a filter over a list rather than a trip to the disk, and the visible rows are derived in one pass by a prefix test, which is correct only because `SpaceRepository.entries` puts a folder immediately before what is inside it. Which folders are **closed** is panel-local, and the set is closed-rather-than-open so a space opens showing what it holds. **The tree shows every file and opens only markdown**: a non-markdown file and a link are drawn muted *and* get no hover at all, because colour is never the only signal — `docs/product/navigation/file-tree/doc.md` gained the three rules that say so, and they wait on the human gate. The panel is the design's explorer to the pixel (the caption, the M2 search field on screen and disabled the way Home draws cloning, rows at 34 pitch indented 16 per level, `▾`/`▸` at 9, the open row in `accent_soft` at weight 600); the top bar now says `repository / folder`, because a space is a folder and three checkouts all have a `docs/`, and the status bar says the space's path with `~` for the home folder plus the open document — the branch, Fetch and Push beside them are M1's and are absent rather than disabled. Two end-to-end scenarios drive the real app against the real fixtures and both pass: one navigates `docs-in-repo` (every file including `logo.svg`, close a folder, open it again, click a document, read the status bar), the other opens a repository at its own root and asserts that neither `.git` nor `HEAD` is anywhere in the tree. 39 tests in `tom_presentation` (97.7% line coverage), 21 in `tom_application`, 47 widget tests in `tom_desktop`. **Four things were tidied on the way, and each is a rule now.** (1) **The spike harnesses are gone** — a spike exists to answer a question, and once the answer is written down the code that produced it is a second entrypoint nobody runs; `re_editor`, `re_highlight` and `markdown` left the app's pubspec with them, because nothing imports a package until the code that needs it exists, and Decisions 18 and 19 say so where they used to point at the harness. (2) **`src/apps/desktop/lib/` is `screens/` + `widgets/` + `bootstrap/` + `theme/`**, and `screens/<feature>/` mirrors `tom_presentation/lib/src/<feature>/` — `screens/home/` beside `home/`, `screens/file_tree/` beside `file_tree/` — so the answer to "where is the UI for this state" is the same path twice; `screens/shell/` is the frame the panels sit in and has no counterpart yet, and `widgets/` holds what more than one screen draws (the wordmark, and `MilestoneChip`, which was the same 30×20 outline in two files). (3) **Every provider is generated** from `riverpod_annotation`: a `@riverpod` function for a seam, a `@riverpod class` for a notifier — which is why the notifiers are now `Home`, `FileTree` and `SpaceSession` (the generator names `homeProvider` after the class) and the session's state is `SpaceSessionState`. The cost is a pin: `riverpod_annotation` 4.0.2 pins `riverpod` 3.2.1 exactly, so the app went from `^3.4.3` to that, and the desktop package grew `build_runner`. Two traps for next time — `select` lives in the runtime package and not in `riverpod_annotation`, and a widget test that mounts two containers in one test leaves a zero-duration disposal timer pending, which fails the test: one container per test is the fix. (4) **A comment is two or three lines** ([layers.md](docs/technical/layers.md#inside-a-package)), exceptions earned rather than assumed; the files this change touches were rewritten to it and the rest of the repository has not been yet. **The markdown preview is built, and with it `tom_infra` holds three of the five capabilities.** Spike B's parser became `markdown_parser/`: the contract takes text and answers **spans** — zero-based inclusive lines, in order, never overlapping, top level, with a blank line in no span — so no `markdown` node reaches the domain and a second parser owes the same format. `markdown_package/` is the implementation, the nineteen `BlockSyntax` subclasses and the `linesToConsume` correction recovered from the spike's own commit. Two things the spike had not had to decide: a construct that runs to the end of its input consumes the blank line after it, so the span gives it back, and the **link reference definitions come from the parser's own map rather than off the text** — a `[d]: url` inside a fence is code, and only the parse knows. `BlockValueObject` is finally code in `tom_domain` (span, source, kind) beside `ParsedDocument`, which carries the blocks *and* the definitions: a block rendered alone would otherwise draw `[text][ref]` literally, and putting a copy on every block is the same string as many times as the document has blocks. `BlockReader` is the port — a port and not a domain service, because splitting markdown is a parser's work — fulfilled by `MarkdownBlockReaderImpl` in `tom_data`, which slices the document's own lines for each span so a block's text stays a view rather than a copy. `ReadDocument` reads then splits, and holds a `DocumentRepositoryFor` typedef for the same reason `GitClientFor` exists: a repository is per space and the space is picked at runtime. **The panel draws one `MarkdownBody` per block** — a widget test counts them — which is the container M2's diff decoration needs; inline markdown is delegated, images resolve against the space and are `Image.file`, a relative `.md` link moves the session, and code blocks are highlighted by `re_highlight` with a language it does not know left unstyled. Two rules of `markdown-preview/doc.md` are **not** met yet and both wait on the same control: preview-only reading mode and its 660/16 measure need the Source · Split · Preview bar, which belongs to source mode; an external link does not open a browser, because that needs a plugin. Next: [source-mode editing](docs/product/editor/source-mode/doc.md) — `re_editor` and the mode bar arrive together, and the bar is what finally gives the preview its reading mode. See `docs/roadmap.md`. There is no user-research phase: the project is built on the maintainer's own experience, stated as such in `docs/roadmap.md`. Desktop wireframes for M0/M1 live in `docs/technical/design/` (skill: `tom-wireframes`). **The identity is decided** ([docs/technical/design/brand.md](docs/technical/design/brand.md)): one shape, a commit on the trunk, used twice — inside a solid document on the app icon (cream tile with a hairline for the dock, sage tile where the tile must carry the colour) and as the O of the wordmark, which is Sora Bold outlines kept as path data so nothing depends on a font. `tools/brand.py` is the decision as code, stdlib only, writing the SVG masters in `design/brand/` and the `brand.svg` sheet, with PNG/`.icns`/`.ico` on demand (`--rasters`) for the runners rather than committed. It was chosen at 16, 32 and 128 pixels on light and dark bars, after some twenty prototypes in Penpot: a `#` reads as a hashtag, a bare node as a magnifier, an outlined page as a box. The wireframes keep "TOM" set in type on purpose — structure, not identity — but the visual design does not: `penpot_build.py` emits the marks into the bundle and `homeEmpty()` places the wordmark by cap height through the kit's new `Scene.svg()`, which rewrites every number before import because Penpot scales geometry but not stroke widths. The Home boards were rebuilt from the bundle, and **every visual-design board is now exported beside its wireframe** as `<screen>-{light,dark}.svg` (18 files, linked from each `doc.md`), through the plugin's `board.export()` and then `tools/embed_fonts.py` — Penpot's export references its own font proxy, which nothing offline can reach (GitHub included), so the script inlines the latin faces as data URIs and the file renders as the board does. Reviewing the boards against the `doc.md` rules found the mocks faithful on layout and wording, with gaps that were then closed **in both generators** (wireframes and visual design, since the wireframe is right on layout): the branch control now sits in the top bar of every screen with a space open (with an M1 chip on M0 screens), Fetch stands beside Push, the commit panel has a "stage all" checkbox, no status bar says "in sync" beside a counter showing commits ahead, reading says "preview only" rather than "read-only", and the sample document is now the top of `visual-language.md` — chosen because it has the image, the code block and the table that markdown-preview says must render and the roadmap never showed. Still not drawn, and recorded as such: the blocked-switch dialog, the post-commit clean state, an opened history entry. **The Penpot file now has a library**: a `Foundations` page built by `penpot_library.js` with the palette as library colours (`light/<role>`, `dark/<role>`), the type scale as typographies (`type/<style>`) and the controls as component masters with variants — `Button` (Kind × Icon, the icon a nested `Icon` component so an instance swaps it with `switchVariant`), `Chip`, `Field`, `Segmented`, `Dot`, `Checkbox`, `Diff mark`, `Branch control`, `Card`, `Popover` — and the screens place instances of them through `Scene.use()`; the kit no longer draws a button. Two Penpot facts the code encodes: `library.components` lists one entry per variant set (the rest are reached through `variants.variantComponents()`), and the plugin draws only on the active page, so the library step switches pages and back. All of it is a draft awaiting the `docs/` human gate.

> Keep this "Current status" section up to date at the end of each meaningful work session — it is what carries context between sessions.
